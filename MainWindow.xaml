<Window x:Class="KocsmaLeltar.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:KocsmaLeltar"
        mc:Ignorable="d"
        Title="Kocsma Leltár Rendszer" Height="500" Width="850"
        MinWidth="800" MinHeight="450"
        WindowStartupLocation="CenterScreen"
        Background="#FF2D2D30"
        d:DataContext="{d:DesignInstance Type=local:MainViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <SolidColorBrush x:Key="PanelBackground" Color="#FF3F3F46"/>
        <SolidColorBrush x:Key="BorderColor" Color="#FF555555"/>
        <SolidColorBrush x:Key="InputBackground" Color="#FF1E1E1E"/>
        <SolidColorBrush x:Key="PrimaryColor" Color="#FF007ACC"/>
        <SolidColorBrush x:Key="DangerColor" Color="#FFD9534F"/>
        <SolidColorBrush x:Key="SecondaryText" Color="#FFAAAAAA"/>

        <Style TargetType="Button">
            <Setter Property="Margin" Value="5,0"/>
            <Setter Property="Padding" Value="15,6"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Background" Value="{StaticResource PrimaryColor}"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="4">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="InputBase" TargetType="Control">
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderColor}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Margin" Value="0,5"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <Style TargetType="TextBox" BasedOn="{StaticResource InputBase}">
            <Setter Property="CaretBrush" Value="White"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="ComboBox" BasedOn="{StaticResource InputBase}">
            <Setter Property="ItemContainerStyle">
                <Setter.Value>
                    <Style TargetType="ComboBoxItem">
                        <Setter Property="Background" Value="{StaticResource InputBackground}"/>
                        <Setter Property="Foreground" Value="White"/>
                    </Style>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="DatePicker" BasedOn="{StaticResource InputBase}">

        </Style>

        <DataTemplate x:Key="LeltarItemTemplate">
            <Border Padding="8" CornerRadius="4">
                <DockPanel LastChildFill="True">
                    <TextBlock x:Name="MennyisegText"
                       DockPanel.Dock="Right"
                       Text="{Binding Mennyiseg, StringFormat={}{0} db}"
                       FontWeight="Bold"
                       VerticalAlignment="Center"
                       Margin="10,0,0,0">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Mennyiseg}" Value="0">
                                        <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                        <Setter Property="TextDecorations" Value="Strikethrough"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                    <StackPanel>
                        <TextBlock x:Name="NevText"
                           Text="{Binding Nev}"
                           FontWeight="Bold"
                           Foreground="White"
                           FontSize="14">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding Mennyiseg}" Value="0">
                                            <Setter Property="Foreground" Value="{StaticResource DangerColor}"/>
                                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                        <TextBlock Text="{Binding Kategoria}" Foreground="{StaticResource SecondaryText}" FontStyle="Italic"/>
                    </StackPanel>
                </DockPanel>
            </Border>
        </DataTemplate>

        <Style TargetType="ListBoxItem">
            <Setter Property="ContentTemplate" Value="{StaticResource LeltarItemTemplate}"/>
            <Setter Property="Margin" Value="2"/>
            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="Bd" Background="Transparent" CornerRadius="4" Padding="2">
                            <ContentPresenter/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF4F4F56"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="#FF3A3A40"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Grid Margin="15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1.5*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0" Background="{StaticResource PanelBackground}" CornerRadius="8" Padding="15" Grid.ColumnSpan="2" Margin="0,0,477,0">
            <DockPanel>
                <Label DockPanel.Dock="Top" Content="Készlet" FontSize="18" FontWeight="Bold" Foreground="White" Margin="0,0,0,10"/>
                <ListBox ItemsSource="{Binding Leltar}" SelectedItem="{Binding KivalasztottItal}" Background="Transparent" BorderThickness="0" SelectionChanged="ListBox_SelectionChanged" Foreground="White" Width="310"/>
            </DockPanel>
        </Border>

        <Border Grid.Column="1" Margin="47,0,14,0" Background="{StaticResource PanelBackground}" CornerRadius="8" Padding="15">
            <DockPanel>
                <StackPanel DockPanel.Dock="Bottom" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,20,0,0">
                    <Button Content="Új felvitel" Command="{Binding UjItalCommand}" Width="80"/>
                    <Button Content="Mentés" Command="{Binding MentesCommand}" Width="80"/>
                    <Button Content="Törlés" Command="{Binding TorlesCommand}" Background="{StaticResource DangerColor}" Click="Button_Click" Width="80"/>
                </StackPanel>

                <Grid DataContext="{Binding KivalasztottItal}">
                    <Grid.Style>
                        <Style TargetType="Grid">
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding}" Value="{x:Null}">
                                    <Setter Property="IsEnabled" Value="False"/>
                                    <Setter Property="Opacity" Value="0.3"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Grid.Style>

                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <Label Content="Név:" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Center"/>
                    <TextBox Grid.Column="1" Text="{Binding Nev, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="1" Content="Kategória:" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Center"/>
                    <ComboBox Grid.Row="1" Grid.Column="1" ItemsSource="{Binding DataContext.Kategoriak, RelativeSource={RelativeSource AncestorType=Window}}" SelectedValue="{Binding Kategoria}" Foreground="Black"/>

                    <Label Grid.Row="2" Content="Mennyiség:" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Mennyiseg, UpdateSourceTrigger=PropertyChanged}"/>

                    <Label Grid.Row="3" Content="Dátum:" Foreground="{StaticResource SecondaryText}" VerticalAlignment="Center"/>
                    <DatePicker Grid.Row="3" Grid.Column="1" SelectedDate="{Binding UtolsoRendelesDatum}" Foreground="Black"/>
                </Grid>
            </DockPanel>
        </Border>
    </Grid>
</Window>